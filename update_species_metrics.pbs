#!/bin/bash
#PBS -l nodes=1:ppn=1
#PBS -l walltime=06:00:00
#PBS -N phylogatr_update_species_metrics
#PBS -j oe
#PBS -A PAS1604
# #PBS -W depend=afterany:10533010

cd $PBS_O_WORKDIR
module load ruby

time bin/rails r 'Occurrence.where(species_total_bytes: nil).distinct.pluck(:species_path).each {|p| Species.update_occurrences p }'
